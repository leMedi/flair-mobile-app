import { connect } from 'react-redux'
import Module from './Module';

const mapDispotachToProps = (dispatch) => ({
})

const mapStateToProps = (state) => ({
})


export default connect(mapStateToProps, mapDispotachToProps)(Module)