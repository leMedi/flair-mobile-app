import { connect } from 'react-redux'
import Dashboard from './Dashboard';

const mapDispotachToProps = (dispatch) => ({
})

const mapStateToProps = (state) => ({
})


export default connect(mapStateToProps, mapDispotachToProps)(Dashboard)