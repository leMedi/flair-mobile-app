import { connect } from 'react-redux'
import Modules from './Modules';

const mapDispotachToProps = (dispatch) => ({
})

const mapStateToProps = (state) => ({
})


export default connect(mapStateToProps, mapDispotachToProps)(Modules)