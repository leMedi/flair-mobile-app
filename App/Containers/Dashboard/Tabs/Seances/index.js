import { connect } from 'react-redux'
import Seances from './Seances';

const mapDispotachToProps = (dispatch) => ({
})

const mapStateToProps = (state) => ({
})


export default connect(mapStateToProps, mapDispotachToProps)(Seances)