import {  StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    marginTop: 20, 
    left: 250, 
    alignItems: 'flex-end', 
    position: 'absolute', 
    justifyContent: 'flex-end'
  }
});